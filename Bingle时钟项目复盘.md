# Bingle时钟 · 项目迭代复盘

> 时间线：2026-09-16 至 2026-09-18 ｜ 共 33 轮迭代
> 项目：Windows 桌面极简番茄钟（Python + PySide6，目录版 exe）
> 当前状态：功能完整、交付目录版（启动约 1 秒）、自启正常、实时时间已加

---

## 第一阶段：需求分析与界面设计（v1–v5 设计稿）

### 第 1 轮 · 项目立项（初始需求）
**用户提出的问题：**
1. 参考 Focus 10 的设计，指出其存在的问题：
   - 设置专注时间很麻烦，每次都要从设置里点进去，希望在专注页面随意调整；
   - 短休息、长休息只要一个"休息"就行，休息时间固定且也要进设置改，流程太固定（先短休、再长休）；
   - 认可 Focus 10 好的点：专注结束后进入"休息"模式，让用户专注完后停下来、知道要休息多久。
2. 最终程序要 exe 形式，直接在 Windows 上运行，支持应用缩放、置顶小窗。
3. 第一步先给界面设计方案。

**结果**：确认核心设计方向（双模式自由切换、时长就地调整、休息手动开始），产出 v1 界面设计方案。

### 第 2 轮 · 设计方案调整 ①（6 点）
1. 界面颜色别太多，至少专注中不要有颜色；
2. 休息不是自动切入，是进入休息界面后手动点开始（避免没正式休息就开始计时）；
3. 专注和休息做成页签，用户可以自己主动切换；
4. 今日 🍅×3 统计不用做；
5. 时间到了要弹窗提示，配提示音乐（找 5 个简单音乐备用）；
6. 状态流转：专注结束自动进入休息、循环不固定；但用户也可直接进入休息（比如只想休息 2 分钟再专注）。

**结果**：产出 v2 设计稿，状态机改为"专注结束只切页签待开始，绝不自动计时"。

### 第 3 轮 · 设计方案调整 ②（4 点）
1. 休息模式的"跳过→专注"功能保留；
2. 界面上的快捷时长提示（"点击数字弹 15/25/30/45/60"等）全部不要，要简洁；
3. 分钟数要可以自己输入，点"＋/－"就在当前基础上 ±5；
4. 设置按钮必须保留（两种模式的铃声设置、置顶、放大缩小都不能拿掉）。

**结果**：产出 v3 设计稿。

### 第 4 轮 · 设计方案调整 ③（2 点）
1. 📌 别针不要用带颜色的按钮，黑白就行，保持简约；
2. 缩小界面要能看到倒计时，支持暂停、重置。

**结果**：产出 v4 设计稿（参照他人小窗设计）。

### 第 5 轮 · 界面文字清理
- "可拖拽 · 右下角缩放 · 双击展开"这行说明不要显示在界面上，写进操作说明文档即可。

**结果**：界面实现零提示文字。

### 第 6 轮 · 按钮命名统一
- "跳过→休息""跳过→专注"两个按钮统一叫"跳过"。

**结果**：统一为单个"跳过"按钮。

### 第 7 轮 · 部署疑问
- 软件开发好后，换到其他电脑上还要安装 Python 等环境吗？

**结果**：确认 PyInstaller 打包成独立 exe，目标机器无需装 Python。

> 穿插事件：期间用户上传过另一个应用（Bingle 工作台）的截图询问 bug，随后澄清"搞错了，你接着处理番茄时钟"，与本项目无关。

---

## 第二阶段：开发交付与视觉打磨（v7 起真机迭代）

### 第 8 轮 · 第一版真机反馈（9 条）
1. 大圆圈颜色改浅，参考截图；
2. 倒计时数字显示不全，＋/－ 分钟、按钮名称都没有，没有测试到位；
3. 暂定、重置改用图标（参考别人做的）；跳过、重置随时可点，不要做控制（当时点不了）；
4. 整体界面放大，太小了；
5. 小别针点了之后窗口消失（置顶 bug）；
6. 缩小窗口样式丑、窗口小、时间不要加粗（主界面也是），字体参考别人做的；
7. 弹窗按钮"开始休息 05:00"改为"开始休息"，休息多久读当前休息时间；提示音循环播放至少 10 秒；点任意按钮关闭弹窗；
8. 应用程序外部边框改成透明效果；
9. 名称改为"Bingle时钟"，但界面顶部不显示。

**结果**：定位并修复置顶 toggle 隐式隐藏窗口 bug；统一图标；界面放大；弹窗/提示音重做；更名 Bingle时钟。

### 第 9 轮 · 界面反馈 ②（2 条）
1. 主界面再放大一倍、支持全屏，倒计时圆圈细一些、倒计时文字字体细一号；
2. 缩小界面内容显示不全、时间字体太大、按钮布局不正常、关闭按钮太小；专注/休息标题文字放中间，样式自查。

**结果**：主窗支持全屏、环细化；小窗布局重排。

### 第 10 轮 · 界面反馈 ③（3 条）
1. 缩小界面增加一个"跳过"按钮；
2. 主界面高度太大，做成正方形；按钮布局不合理、太靠下、配色不舒服；
3. 修改时间时直接选中当前时间，不用再敲一次回车。

**结果**：主窗改 760×760 正方形；小窗补跳过按钮；时长输入聚焦即全选。

### 第 11 轮 · 界面反馈 ④（3 条）
1. 主界面布局重做：倒计时区域放大；开始按钮用圆形按钮框（同另外两个）；加减分钟放到倒计时右侧边，"分钟"两个字不要；
2. 软件还是要有一个淡边框颜色（白色背景下好分辨）；
3. 时间到弹窗位置放到屏幕右下角。

**结果**：± 按钮改为贴环右侧、去掉"分钟"字样；加淡边框 #E1E3E7；弹窗右下角定位。

### 第 12 轮 · 界面反馈 ⑤（4 条）
1. 倒计时区域再放大一倍，文字跟着放大，区域要随窗口自适应（窗口调大/最大化时同步放大）；
2. ± 分钟位置往右移（当时贴到倒计时上）；
3. 底部三个按钮放大；
4. 布局和美观问题要自己多测试、截图看效果。

**结果**：定位布局根因——环默认 sizePolicy 不拉伸，改为 Expanding + 2:3:2 弹性布局；建立截图验收机制。

### 第 13 轮 · 弹窗反馈
- 弹窗内容布局居中一下，关闭按钮放大一些（当时小到点不到）。

**结果**：弹窗内容居中、关闭按钮加大。

### 第 14 轮 · 界面反馈 ⑥（2 条）
1. 圆圈再放大一些且居中；
2. ± 时间按钮不要放最右侧，适当左移、离圆圈近一些。

**结果**：环尺寸与居中调整；± 按钮手动定位贴环右缘 20px。

### 第 15 轮 · 界面反馈 ⑦（2 条）
1. ± 时间按钮位置不对（贴到环上）；
2. 底部按钮再稍微往上一点。

**结果**：修复手动定位时序（先 activate 布局再取几何），± 间距调至 40px。

### 第 16 轮 · 界面反馈 ⑧（2 条）
- 底部按钮再往上一点；圈圈再放大一点。

**结果**：继续微调。

### 第 17 轮 · 界面反馈 ⑨
- 圈圈太大了，太丑了（上一轮放得过大）。

**结果**：环尺寸回退到窗口 60% 定稿值（456px）。

### 第 18 轮 · 界面反馈 ⑩
- 加减时间按钮再往左右移动一点位置。

**结果**：± 按钮位置精调。

### 第 19 轮 · 间距精调
- ± 按钮整体右移：与环的间距调到 40px。

**结果**：间距定稿 40px。

### 第 20 轮 · 按钮精调
- 底部 3 个按钮再上移 20px，再放大一点。

**结果**：底部按钮上移、放大至 64px。

### 第 21 轮 · 大需求（4 项）
1. 缩小按钮单独放到左边，图标换一个款式（参考画中画图标）；
2. 底部按钮图标再放大一些，中间的开始按钮用蓝色实心填充；
3. 全部处理完后整理设计说明（软件设计架构层面、注意事项、优化建议）；
4. 重新做系统瘦身，把不要的文件清理掉（已做过备份）。

**结果**：缩小按钮移到左上并换画中画图标；开始按钮实心蓝（休息绿）；产出《Bingle时钟设计说明.md》；清理全部参考图/测试脚本/旧设计稿。

### 第 22 轮 · 功能完善（2 项）
1. 缩小界面的 × 换成"放大"按钮；
2. 操作加减分钟按钮时，如果正在倒计时，改为在当前剩余时间基础上 ±5，不打断、不重置倒计时。

**结果**：小窗右上改为放大按钮（与双击同行为）；新增 nudge_minutes 逻辑（倒计时中/暂停改剩余、待开始改总时长，下限钳制 1 分钟）。

### 第 23 轮 · 体积疑问 + 顶栏优化
1. 清理了为什么安装包还有 50 多 M？
2. 优化：主界面关闭 × 左边加"最小化"按钮；设置按钮放到左边（参考别人的设计）。

**结果**：定位 PySide6 全量打包（含 194MB WebEngine、FFmpeg、软件 OpenGL）问题，写瘦身 spec 白名单过滤，体积 57.6→36.6MB；顶栏改为左[设置]、右[缩小][置顶][全屏][最小化][关闭]（缩小按钮放在置顶左边）。

---

## 第三阶段：技术答疑与文档沉淀

### 第 24 轮 · 架构讲解请求
- 项目用什么框架、什么技术做的；界面怎么做；数据如何交互、前后台如何响应；数据、日志怎么管理。

**结果**：输出完整架构说明（PySide6/信号槽/状态机/monotonic 计时/JSON 配置/日志现状与建议），并附架构数据流图。

### 第 25 轮 · 搜索：PySide6
- 了解 PySide6 框架本身。

### 第 26 轮 · 搜索：Qt6 的 C++ GUI
- 了解 Qt6 C++ GUI 与本项目的关系。

### 第 27 轮 · 文档补充
- 之前提到的核心产品规则 6 点整理到说明文档里。

**结果**：核对并补强设计说明——第 5 点补充"倒计时中 ±5 不打断"，新增"2.6 数据与日志管理"章节，修正音频实现描述（QMediaPlayer 而非 QSoundEffect）。

---

## 第四阶段：项目迁移与维护

### 第 28 轮 · 工作目录迁移
- 项目迁入 `E:\Code\comate-zulu-demo\Bingle时钟`，以后不再在 C 盘旧目录操作。

**结果**：确认迁移完整性（源码 10 模块、最新 exe、两份文档均在）。

### 第 29 轮 · 开机自启检查
- 检查开机启动设置是否需要修改。

**结果**：注册表 Run 项原指向 C 盘旧路径，已更新为新路径；确认代码用 sys.executable 动态取路径、逻辑自适应。

### 第 30 轮 · 开机自启失效排查
- 没有设置开启自动启动吗，刚重启电脑没有生效。

**结果**：定位根因——迁移时 exe 文件名损坏为 `Bingle钟.exe`（少了"时"字），与注册表指向不一致；改回 `Bingle时钟.exe` 并验证可正常启动。

### 第 31 轮 · 缩小模式优化 + 启动提速（当前）
1. 小窗时间数字要更显眼（最重要的信息）；专注模式开始图标颜色太突出，参考主界面调淡；底部三个按钮缩小一些；
2. "专注"文字没有居中；
3. 应用程序启动慢，什么原因、能否优化；
4. 主界面"缩小"按钮放回右边，放在置顶按钮左边。

**结果**：
- 小窗时间字号加大（side*0.24）；开始按钮改淡蓝/淡绿；底部按钮 36→32px；
- 模式名严格居中；
- 启动根因：单文件模式每次自解压，实测 5-7 秒；对比 onedir 目录版约 1 秒（快 5-7 倍），定案采用目录版；
- 顶栏顺序调整为左[设置]、右[缩小][置顶][全屏][最小化][关闭]。

### 第 32 轮 · 弹窗增强 + 回车直开 + 启动定案（6 条）
1. 缩小模式：时间数字更显眼；专注开始图标颜色太突出，参考主界面调淡；底部三个按钮缩小（与上轮 1-4 合并确认）；
2. "专注"文字没居中（已在上轮修复，本轮确认）；
3. 启动慢要优化（上轮已分析，本轮定案）；
4. 缩小按钮放回右边、置顶左边（已在上轮修复，本轮确认）；
5. 弹窗增强：新增"再休息一会"按钮（点击继续休息倒计时）；"继续专注"按钮不好看，加点棱角、按钮大小差不多；弹窗未关闭时用户已在主界面操作，此时点关闭/稍后等按钮不要再更新主界面状态；
6. 修改倒计时数字后，按回车直接进入倒计时。

**结果**：
- 弹窗：休息结束新增"再休息一会"（留休息模式重新开始倒计时）；按钮统一圆角 6px 棱角 + 等大（min-width 88px）；新增防覆盖机制——状态机加 `mutation_count` 用户操作版本号，弹窗弹出时记录快照，期间用户操作过则弹窗按钮只停音关窗、不改状态；
- 回车直开：`returnPressed` 提交输入并直接 `start()`；
- 启动定案：正式切换 onedir 目录版（86.7MB、启动约 1 秒），自启注册表更新为新路径，旧单文件 exe 删除，单文件配置备份 `Bingle时钟_onefile.spec.bak` 可一键重建。

### 第 33 轮 · 主界面加实时时间
- 倒计时下方加一个当前实时时间，小字体显示（参考效果图：铃铛图标 + HH:MM）。

**结果**：环中心倒计时下方新增小字实时时间（新增 bell_icon 铃铛图标 + 12px 灰色 HH:MM，独立 QTimer 每秒刷新）。

---

## 复盘总结

| 维度 | 数据 |
|---|---|
| 迭代轮次 | 33 轮（其中界面视觉打磨约 15 轮） |
| 关键 bug 修复 | 置顶隐式隐藏、环不拉伸、布局重入、手动定位时序、配置 BOM、exe 文件名损坏、弹窗状态覆盖 |
| 用户核心诉求 | 极简黑白、自由切换、绝不自动开始下一阶段、就地调时长、启动快 |
| 工程沉淀 | 源码 10 模块 + 瘦身 spec + 使用说明 + 设计说明 + 复盘文档 + 备份目录 |
| 当前形态 | 目录版 onedir（86.7MB、启动约 1 秒）；单文件版配置备份可重建 |
