# Bingle时钟 · 使用说明

极简双模式番茄钟：专注 / 休息自由切换，时长就地调整，时间到只提醒、绝不自动开始下一阶段。

---

## 一、主窗口

```
┌────────────────────────────┐
│ ⚙ ▣      ⧉ 📌 ⛶ ─ ×       │   ← 左上：设置/统计；右上：缩小/置顶/全屏/最小化/关闭
│     [ 专注 ]   [ 休息 ]     │   ← 页签，随时点击切换模式
│          ╭──────╮           │
│          │  25  │           │   ← 点击数字可直接输入分钟（1-180）
│          ╰──────╯           │
│              [＋]           │   ← 每次 ±5 分钟
│              [－]           │
│         ⟳     ▶     ⟲      │   ← 重置 / 开始暂停 / 跳过
└────────────────────────────┘
```

| 操作 | 说明 |
|---|---|
| 页签「专注 / 休息」 | 随时手动切换，不受流程限制 |
| 点击中间数字 | 直接输入想要的分钟数（1～180），**按回车直接开始倒计时** |
| ＋ / － 按钮 | 在当前时长基础上 ±5 分钟 |
| ▶ 开始 / ⏸ 暂停 | 同一个按钮，按当前状态切换 |
| ⟲ 跳过 | 中止当前阶段，直接进入另一模式（待开始，不计时） |
| ⟳ 重置 | 回到当前模式的初始时长 |
| ⚙ 设置 | 打开设置面板（提示音 / 音量 / 开机自启） |
| ▣ 统计 | 打开专注统计窗口（今日/本周/累计 + 近 7 天柱状图） |
| ─ 最小化 | 最小化到任务栏 |
| ⛶ 全屏 | 铺满整个屏幕，再点一次还原窗口 |
| × 关闭 | 退出程序 |

> 跳过、重置**随时可点**，不受计时状态限制。
> 设置过的时长会记住，下次打开还是上次的值（专注、休息分开记忆）。
> 窗口无边框圆角，按住空白处可拖动，右下角可拖拽缩放。
> 倒计时中按 ＋/－ 会在**当前剩余时间**上 ±5，不打断倒计时。

## 二、时间到之后

- 弹出右下角置顶的弹窗，并**循环播放提示音（至少 10 秒）**，确保听得见。
- **绝不会自动开始下一阶段**——倒计时结束后停下来等你。
- 弹窗按钮：
  - **开始休息**（专注结束）：切到休息模式并直接开始倒计时（休息时长用设置里的休息时间）；
  - **继续专注**（休息结束）：切到专注模式并直接开始倒计时；
  - **再休息一会**（休息结束）：留在休息模式，重新开始一轮休息倒计时；
  - **稍后**：只切到另一模式，停在待开始状态；
  - **×**：关闭弹窗，留在当前页签；
  - 点任何一个按钮，弹窗关闭、提示音停止。
- 弹窗开着时若你已在主界面开始/调整过倒计时，再点弹窗按钮**只会关弹窗停音，不会覆盖你的操作**。

## 三、置顶小窗

点击主窗口左上角第一个图标（⧉），主窗口缩成一个始终置顶的小窗，可一边工作一边看倒计时。

```
┌──────────────┐
│ ● 休息     ⛶ │
│   ╭────╮     │
│   │05:00│    │   ← 环形进度 + 大号时间
│   ╰────╯     │
│  ⟳   ▶   ⟲  │   ← 重置 / 播放暂停 / 跳过
└──────────────┘
```

| 操作 | 说明 |
|---|---|
| 小窗任意位置按住拖动 | 移到屏幕任意角落 |
| 右下角拖拽 | 缩放小窗大小 |
| 双击小窗 | 展开回主窗口 |
| 右上角 ⛶ 放大按钮 | 展开回主窗口 |
| ▶ 播放 / 暂停 | 和主窗口的开始/暂停同一逻辑 |
| ⟳ 重置 | 回到初始时长 |
| ⟲ 跳过 | 切到另一模式（待开始） |
| 环形进度 | 显示当前阶段剩余进度（休息时为绿色） |

## 四、置顶（图钉 📌）

点击主窗口右上角第一个图标，窗口置顶（始终在最前面），再点一次取消。置顶激活时按钮黑底反白。置顶状态会记住。

## 五、专注统计（▣）

点击主窗口左上角第二个图标（▣），打开统计窗口，查看专注时长：

- **今日 / 本周 / 累计** 三张卡片 + **近 7 天柱状图**；
- **只统计自然走完的专注**：跳过、重置、中途暂停都不计；
- 窗口打开期间**每 5 秒自动刷新**，专注走完回到窗口即可看到最新数据；
- 数据保存在本地配置文件中，不联网、不收集任何信息。

## 六、设置（齿轮 ⚙）

| 项 | 说明 |
|---|---|
| 专注完成提示音 | 5 款音效任选，可试听 |
| 休息完成提示音 | 5 款音效任选，可试听 |
| 音量 | 滑块 0-100 |
| 开机自启 | 开关（写入注册表启动项） |

## 七、提示音

内置 5 款免费商用提示音（Mixkit 授权，无署名要求）：

1. Happy Bells（清脆双铃）
2. Bell Notification（经典单铃）
3. Flute Melody（柔和长笛）
4. Flute Uplifting（轻快笛声）
5. Magic Ring（魔法铃）

## 八、数据存放

- 配置文件：`%APPDATA%\PomodoroTimer\config.json`（时长、铃声、音量、置顶、开机自启、专注统计）
- 删除该文件即恢复默认：专注 25 分钟 / 休息 5 分钟 / 音量 80（统计记录也一并清空）
- 提示音已打包在 exe 内，不需要额外文件

## 九、卸载

Bingle时钟是绿色应用（目录版），删除整个 `Bingle时钟` 文件夹即可；如需彻底清理，再删除 `%APPDATA%\PomodoroTimer` 文件夹；若开了开机自启，先在设置里关掉。

> 目录版包含 `Bingle时钟.exe` 和 `_internal` 文件夹，两者要整体保留、一起移动，不能只拷 exe。
