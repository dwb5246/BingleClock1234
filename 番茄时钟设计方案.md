# 番茄时钟 · 最终设计方案 v1.0

> 2026-09-16 定稿 · Windows 桌面应用（.exe）

## 一、产品概述

- **平台**：Windows 10/11，最终交付单文件 exe
- **技术栈**：Python 3.14 + PySide6（Qt6）+ PyInstaller
- **定位**：极简番茄钟。专注 / 休息双模式页签自由切换，倒计时结束提醒休息（绝不自动开始下一阶段）

## 二、核心设计原则

1. **极简**：界面零提示文字；全应用只用黑白灰 + 休息态一个绿色强调色
2. **手动优先**：任何倒计时结束都只弹窗 + 跳转待开始，必须用户手动点"开始"才计时
3. **就地调整**：时长在页面上直接改（点击输入 / ±5），不设调时长的设置入口

## 三、界面设计

### 1. 主窗口（约 320×420，可自由缩放，最小 300×380）

```
┌──────────────────────────────┐
│                    [图钉][小窗][齿轮] │  顶栏：黑白线条图标，置顶激活时反白
│         专注    休息                │  页签：激活=黑字加粗+下划线（休息=绿）
│         ┌──────────┐               │
│         │   25      │  点击数字→直接输入分钟│
│         └──────────┘   [- 分钟 +] ±5 步进 │
│   [▶ 开始]  [跳过]  [重置]             │  开始↔暂停；跳过=切另一模式；重置=回待开始
└──────────────────────────────┘
```

| 元素 | 说明 |
|---|---|
| 顶栏右上 | 置顶（图钉）、缩小成置顶小窗、设置（齿轮）；黑白线条 SVG，置顶激活时图标反白 |
| 页签 | 「专注｜休息」居中；激活页签黑色加粗 + 底部横线（休息横线为绿色） |
| 计时区 | 圆形进度环 + 大号分钟数；专注=黑白，休息=进度环绿色；数字可点击输入，`−`/`＋` 步进 5 |
| 操作区 | 开始/暂停（待开始黑实心"开始"，计时中空心"暂停"）、跳过、重置 |

### 2. 置顶小窗（无边框，约 170×140，可缩放 140×110 ~ 320×240）

```
┌───────────────┐
│ ● 专注      ×  │  模式色点 + 模式名 + 关闭
│    25:00      │  大号倒计时，字号随窗口等比缩放
│ ▁▁▁▁▁▁▂▂     │  细进度条（当前阶段剩余比例）
│   ▶    ⟳     │  播放/暂停 + 重置（黑白圆形按钮）
└───────────────┘
```

- 系统置顶、可拖拽移动、右下角拖拽缩放、双击展开回主窗口
- 专注态黑白；休息态色点与进度条变绿
- 空闲显示 ▶（点开始），计时中显示 ⏸
- 界面不显示任何操作说明文字（说明见文档第七节）

### 3. 时间到弹窗（独立置顶小窗，自动播放提示音）

```
┌────────────────────────┐
│ ⏰ 专注时间到        × │
│ 已专注 25 分钟，休息一下吧 │
│ [▶ 开始休息 5:00] [稍后] │
└────────────────────────┘
```

| 按钮 | 行为 |
|---|---|
| 开始休息 | 用户手动确认 → 跳到休息页签并直接开始休息倒计时 |
| 稍后 | 跳到休息页签待开始，不计时 |
| × | 只关闭弹窗，留在当前页签 |

专注结束弹窗按钮为"开始休息"；休息结束弹窗按钮为"继续专注"。

### 4. 设置面板（齿轮打开）

| 项 | 说明 |
|---|---|
| 专注完成提示音 | 5 款音效任选 + 试听 |
| 休息完成提示音 | 5 款音效任选 + 试听 |
| 音量 | 滑块（0-100） |
| 开机自启 | 开关 |

## 四、交互规则

1. **时长调整**：点击分钟数字 → 变输入框直接输入（1~180 分钟，回车生效）；`−`/`＋` 在当前值 ±5；**计时中修改时长 = 重置回待开始**
2. **页签切换 / 跳过**：任意时刻可点页签或"跳过"切换到另一模式（中止当前倒计时，目标模式为待开始）
3. **时长记忆**：专注、休息时长各自独立持久化，重启恢复
4. **状态流转**：

```
专注·待开始 ──开始──▶ 专注计时中 ──时间到──▶ 弹窗+铃声 ──▶ 休息·待开始
休息·待开始 ──开始──▶ 休息计时中 ──时间到──▶ 弹窗+铃声 ──▶ 专注·待开始
任意状态：暂停/继续、跳过（切另一模式）、重置（回本模式待开始）
规则：倒计时结束绝不自动开始下一阶段；可随时从任一模式开始
```

5. **计时精度**：基于系统时间戳（monotonic）计算剩余秒数，UI 定时器只负责刷新，卡顿 / 休眠唤醒不走偏

## 五、提示音（5 款，Mixkit 免费商用授权）

| 编号 | 文件 | 时长 | 风格 |
|---|---|---|---|
| 1 | 01-happy-bells.mp3 | 3s | 清脆双铃声 |
| 2 | 02-bell-notification.mp3 | 3s | 经典单铃声 |
| 3 | 03-flute-melody.mp3 | 7s | 柔和长笛旋律 |
| 4 | 04-flute-uplifting.mp3 | 3s | 轻快上扬笛声 |
| 5 | 05-magic-ring.mp3 | 3s | 钢琴魔法铃 |

默认专注音 = 1，默认休息音 = 3。

## 六、技术方案

| 模块 | 实现 |
|---|---|
| 主窗口 | QWidget，自定义 paintEvent 绘制圆形进度环 |
| 小窗 | `Qt.FramelessWindowHint \| Qt.WindowStaysOnTopHint` + 半透明圆角；鼠标事件实现拖拽，右下角热区实现缩放；双击展开 |
| 状态机 | `mode ∈ {focus, break}` × `state ∈ {idle, running, paused}`，单一状态源驱动全部 UI |
| 持久化 | JSON 配置文件存于用户目录（时长、音量、铃声选择、置顶状态） |
| 铃声 | QMediaPlayer 播放内嵌资源 |
| 弹窗 | 独立 frameless 置顶窗口，不依赖 Windows 通知中心 |
| 打包 | PyInstaller `--onefile --windowed`，音效打包进 exe |

## 七、操作说明（不显示在界面，随发布文档提供给用户）

- **小窗**：按住窗口任意位置拖动；右下角拖拽缩放；双击展开回主窗口；始终置顶
- **图标**：图钉 = 窗口置顶开关；小窗 = 缩成置顶小窗；齿轮 = 设置
- **跳过** = 中止当前阶段，直接进入另一模式（待开始）
- **倒计时结束只弹窗提醒，不会自动开始**，需要自己点"开始"
