# Bingle 时钟・项目阶段性总结

> 范围：2026-09-16 至 09-18，33+ 轮迭代，从需求到交付的完整桌面应用项目
> 本文从三个角度提炼：①可复用资产 ②流程复盘（结合 AI/vibe coding 模式）③下一步



***

## 一、可复用的开发模板、方法、Skill

### 1. 三段式交付流程（本项目最值钱的资产）

**需求 → 高保真 HTML 设计稿 → 真机实现**



* 先做 HTML 设计稿迭代界面（零成本、浏览器直接预览、用户随时改），界面定稿后再写代码；

* 本项目中 v1→v5 设计稿阶段解决了 80% 的界面争议，代码阶段只做 "按图施工"。

* **适合所有 GUI 项目**：界面和逻辑是两个变更域，先定界面，逻辑返工率大幅下降。

### 2. 截图验收闭环（视觉迭代黄金工作流）



```
改代码 → 真平台 widget.grab() 出预览图 → 自己先看 → 交付附预览图 → 用户截图反馈 → 数值化定位 → 改参数
```



* 关键经验：把用户的模糊反馈翻译成**可执行参数**（"间距 40px"" 上移 20px""按钮 64px"），一锤定音；

* 三原则：**真平台验证**（offscreen 无字体，白测）、**环境隔离**（测试改 APPDATA 到临时目录，不污染真实配置）、**每次交付带预览图**（让用户 "看见" 而不是 "想象"）。

### 3. 可复用的工程模式（从本项目源码提炼）



| 模式          | 做法                                                    | 适用               |
| ----------- | ----------------------------------------------------- | ---------------- |
| 状态机 + 信号槽解耦 | `state.py` 纯逻辑无 UI 依赖，界面订阅信号刷新                        | 任何带计时 / 多状态 UI   |
| 单调时钟计时      | `deadline = time.monotonic() + remaining`，UI 定时器只刷新显示 | 计时类应用，不漂移        |
| 操作版本号防覆盖    | `mutation_count` 记录用户操作，弹窗回调前比对，不一致则只关窗不改状态           | 弹窗 / 异步交互覆盖主界面状态 |
| 参数化布局       | 环占窗口 60%、± 贴环 40px、按钮 64px—— 尺寸全部数值化                  | 界面反复微调场景         |
| 瘦身 spec 白名单 | PyInstaller 按 DLL / 插件 / 翻译白名单过滤 Qt 全家桶，55MB→37MB     | Qt 打包体积优化        |
| 交付形态用数据决策   | 实测单文件 5-7s vs 目录版 1s，用数据让用户选                          | 一切技术选型           |

### 4. 文档四件套（可沉淀为 Skill）



* **使用说明**（用户视角）＋**设计说明**（维护视角）＋**项目复盘**（迭代视角）＋**用户输入原文**（需求追溯）

* 价值：每次改动实时同步文档，文档就是 AI 的 "外部记忆"—— 长对话压缩后靠它找回上下文，不丢细节。

### 5. 可固化的检查清单（打包 / 交付）



* 打包前清残留进程 → 打包（PyInstaller exit code 1 属正常）→ 中文文件名勿改动（曾因少 "时" 字致自启失效）→ 注册表自启路径与 exe 一致 → 真机启动计时验证。



***

## 二、做得好 & 可优化（结合 AI coding /vibe coding 模式）

### 做得好的



1. **设计稿先行**：把 "审美争议" 拦在代码之前，避免边写边改；

2. **每轮交付预览图 + 数值化反馈**：迭代效率高，用户满意度曲线上升快；

3. **文档实时沉淀**：到后期无论怎么改，上下文没丢；

4. **核心诉求抓得准**：极简、不自动计时、自由切换 —— 所有迭代围绕这三点，没跑偏。

### 可优化的（按重要度排序）



1. **界面微调轮次过多（15+ 轮），根因是缺少 "设计规范"**

* 现象：圆圈大小、± 按钮位置、按钮上移量来回调；

* 本质：审美靠 "每轮重排"，而不是 "调参数"；

* 改进：项目一开始就建立**设计 token 表**（色板 / 字号 / 间距 / 尺寸常量），所有反馈落到参数上。这是 vibe coding 最重要的教训：**AI 每次 "重做布局" 是高风险高成本动作，参数化才能收敛**。

1. **阶段间缺硬验收标准**

* 设计稿阶段没设 "对照参考图逐项打分" 的关卡，导致视觉问题漏到实现阶段；

* 改进：每个阶段结束前用 checklist（居中？间距？字号？对比度？）让用户确认，确认才进下一阶段。

1. **主观描述没有主动给选项收敛**

* "颜色调淡"" 加点菱角 " 这类模糊词，早期可给 2-3 个候选让用户选，而不是猜（但别过度追问 —— 用户要简洁，默认给值 + 说明可改最合适）。

1. **异步交互问题在设计阶段被忽略**

* 弹窗未关用户已在主界面操作 → 状态被覆盖，是运行中才发现的；

* 改进：涉及**弹窗 / 多窗口 / 后台操作**时，先画一张交互状态图再动手。

1. **部署形态的验收标准定晚了**

* 体积、启动速度、自启是后期才集中处理；

* 改进：项目启动时就把 "交付形态" 目标写进需求（单文件 or 目录、体积上限、启动目标秒数）。

### 对 vibe coding 模式的总体判断



* **优势在本项目体现充分**：快速原型、视觉即改即看、需求随时变，传统瀑布流做不到；

* **风险也暴露无遗**：无规范 → 反复调参；AI 每次 "重构" 引入回归（环大小被调大调小又调回）；长上下文压缩丢细节；

* **结论**：vibe coding 需要 "工程化护栏"—— 设计 token 先行 + 截图验收闭环 + 文档外部记忆 + 数值化沟通 + 变更隔离（样式参数集中一处）。做到这五条，vibe coding 从 "碰运气" 变成 "可控的高效"。



***

## 三、下一步建议

### 功能层（按价值排序）



1. **单实例锁**：防多开（当前可开多个实例，状态不同步）；

2. **系统托盘**：关闭改为最小化到托盘，减少误退，托盘右键可快速开始 / 暂停；

3. **自选铃声目录**：支持加载用户自己的音频文件；

4. **高分屏适配**：补 Per-Monitor DPI 测试；

5. **统计功能（可开关）**：专注次数 / 时长统计，默认关闭（用户最初拒绝，做成可选项）。

### 工程层



1. **加日志**：`logging + RotatingFileHandler` 写 `%APPDATA%\PomodoroTimer\logs`，加 `sys.excepthook` 崩溃兜底 —— 当前无日志，出问题难排查；

2. **自动化测试**：至少给 `state.py` 状态机写单元测试（计时、±5、防覆盖逻辑），打包前跑一遍；

3. **把设计 token 表写进设计说明**：为将来换肤 / 维护做准备。

### 产品层



1. 若要分发 / 装机：加应用图标美化、代码签名、用 Inno Setup 做安装器（顺带解决目录版分发体验）；

2. 若做 v2：界面迭代成本高的痛点可考虑 QML 或 Web 壳（Tauri），但**当前 PySide6 已够用，不建议重写**—— 除非要上复杂的自定义界面动画。

### 一句话总结

**这个项目的最大价值不是番茄钟本身，而是验证了一套 "设计稿先行 + 截图验收 + 参数化调优 + 文档记忆" 的 vibe coding 方法论 —— 下一项目直接复用，能省掉至少一半的迭代轮次。**